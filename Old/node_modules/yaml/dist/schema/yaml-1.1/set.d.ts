import { Pair } from 'yaml/dist/nodes/Pair';
import type { Scalar } from 'yaml/dist/nodes/Scalar';
import type { ToJSContext } from 'yaml/dist/nodes/toJS';
import { YAMLMap } from 'yaml/dist/nodes/YAMLMap';
import type { Schema } from 'yaml/dist/schema/Schema';
import type { StringifyContext } from 'yaml/dist/stringify/stringify';
import type { CreateNodeContext } from 'yaml/dist/util';
import type { CollectionTag } from 'yaml/dist/schema/types';
export declare class YAMLSet<T = unknown> extends YAMLMap<T, Scalar<null> | null> {
    static tag: string;
    constructor(schema?: Schema);
    add(key: T | Pair<T, Scalar<null> | null> | {
        key: T;
        value: Scalar<null> | null;
    }): void;
    /**
     * If `keepPair` is `true`, returns the Pair matching `key`.
     * Otherwise, returns the value of that Pair's key.
     */
    get(key: unknown, keepPair?: boolean): any;
    set(key: T, value: boolean): void;
    /** @deprecated Will throw; `value` must be boolean */
    set(key: T, value: null): void;
    toJSON(_?: unknown, ctx?: ToJSContext): any;
    toString(ctx?: StringifyContext, onComment?: () => void, onChompKeep?: () => void): string;
    static from(schema: Schema, iterable: unknown, ctx: CreateNodeContext): YAMLSet;
}
export declare const set: CollectionTag;

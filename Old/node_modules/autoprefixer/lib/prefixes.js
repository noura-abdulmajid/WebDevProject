let vendor = require('autoprefixer/lib/vendor')
let Declaration = require('autoprefixer/lib/declaration')
let Resolution = require('autoprefixer/lib/resolution')
let Transition = require('autoprefixer/lib/transition')
let Processor = require('autoprefixer/lib/processor')
let Supports = require('autoprefixer/lib/supports')
let Browsers = require('autoprefixer/lib/browsers')
let Selector = require('autoprefixer/lib/selector')
let AtRule = require('autoprefixer/lib/at-rule')
let Value = require('autoprefixer/lib/value')
let utils = require('autoprefixer/lib/utils')
let hackFullscreen = require('autoprefixer/lib/hacks/fullscreen')
let hackPlaceholder = require('autoprefixer/lib/hacks/placeholder')
let hackPlaceholderShown = require('autoprefixer/lib/hacks/placeholder-shown')
let hackFileSelectorButton = require('autoprefixer/lib/hacks/file-selector-button')
let hackFlex = require('autoprefixer/lib/hacks/flex')
let hackOrder = require('autoprefixer/lib/hacks/order')
let hackFilter = require('autoprefixer/lib/hacks/filter')
let hackGridEnd = require('autoprefixer/lib/hacks/grid-end')
let hackAnimation = require('autoprefixer/lib/hacks/animation')
let hackFlexFlow = require('autoprefixer/lib/hacks/flex-flow')
let hackFlexGrow = require('autoprefixer/lib/hacks/flex-grow')
let hackFlexWrap = require('autoprefixer/lib/hacks/flex-wrap')
let hackGridArea = require('autoprefixer/lib/hacks/grid-area')
let hackPlaceSelf = require('autoprefixer/lib/hacks/place-self')
let hackGridStart = require('autoprefixer/lib/hacks/grid-start')
let hackAlignSelf = require('autoprefixer/lib/hacks/align-self')
let hackAppearance = require('autoprefixer/lib/hacks/appearance')
let hackFlexBasis = require('autoprefixer/lib/hacks/flex-basis')
let hackMaskBorder = require('autoprefixer/lib/hacks/mask-border')
let hackMaskComposite = require('autoprefixer/lib/hacks/mask-composite')
let hackAlignItems = require('autoprefixer/lib/hacks/align-items')
let hackUserSelect = require('autoprefixer/lib/hacks/user-select')
let hackFlexShrink = require('autoprefixer/lib/hacks/flex-shrink')
let hackBreakProps = require('autoprefixer/lib/hacks/break-props')
let hackWritingMode = require('autoprefixer/lib/hacks/writing-mode')
let hackBorderImage = require('autoprefixer/lib/hacks/border-image')
let hackAlignContent = require('autoprefixer/lib/hacks/align-content')
let hackBorderRadius = require('autoprefixer/lib/hacks/border-radius')
let hackBlockLogical = require('autoprefixer/lib/hacks/block-logical')
let hackGridTemplate = require('autoprefixer/lib/hacks/grid-template')
let hackInlineLogical = require('autoprefixer/lib/hacks/inline-logical')
let hackGridRowAlign = require('autoprefixer/lib/hacks/grid-row-align')
let hackTransformDecl = require('autoprefixer/lib/hacks/transform-decl')
let hackFlexDirection = require('autoprefixer/lib/hacks/flex-direction')
let hackImageRendering = require('autoprefixer/lib/hacks/image-rendering')
let hackBackdropFilter = require('autoprefixer/lib/hacks/backdrop-filter')
let hackBackgroundClip = require('autoprefixer/lib/hacks/background-clip')
let hackTextDecoration = require('autoprefixer/lib/hacks/text-decoration')
let hackJustifyContent = require('autoprefixer/lib/hacks/justify-content')
let hackBackgroundSize = require('autoprefixer/lib/hacks/background-size')
let hackGridRowColumn = require('autoprefixer/lib/hacks/grid-row-column')
let hackGridRowsColumns = require('autoprefixer/lib/hacks/grid-rows-columns')
let hackGridColumnAlign = require('autoprefixer/lib/hacks/grid-column-align')
let hackPrintColorAdjust = require('autoprefixer/lib/hacks/print-color-adjust')
let hackOverscrollBehavior = require('autoprefixer/lib/hacks/overscroll-behavior')
let hackGridTemplateAreas = require('autoprefixer/lib/hacks/grid-template-areas')
let hackTextEmphasisPosition = require('autoprefixer/lib/hacks/text-emphasis-position')
let hackTextDecorationSkipInk = require('autoprefixer/lib/hacks/text-decoration-skip-ink')
let hackGradient = require('autoprefixer/lib/hacks/gradient')
let hackIntrinsic = require('autoprefixer/lib/hacks/intrinsic')
let hackPixelated = require('autoprefixer/lib/hacks/pixelated')
let hackImageSet = require('autoprefixer/lib/hacks/image-set')
let hackCrossFade = require('autoprefixer/lib/hacks/cross-fade')
let hackDisplayFlex = require('autoprefixer/lib/hacks/display-flex')
let hackDisplayGrid = require('autoprefixer/lib/hacks/display-grid')
let hackFilterValue = require('autoprefixer/lib/hacks/filter-value')
let hackAutofill = require('autoprefixer/lib/hacks/autofill')

Selector.hack(hackAutofill)
Selector.hack(hackFullscreen)
Selector.hack(hackPlaceholder)
Selector.hack(hackPlaceholderShown)
Selector.hack(hackFileSelectorButton)
Declaration.hack(hackFlex)
Declaration.hack(hackOrder)
Declaration.hack(hackFilter)
Declaration.hack(hackGridEnd)
Declaration.hack(hackAnimation)
Declaration.hack(hackFlexFlow)
Declaration.hack(hackFlexGrow)
Declaration.hack(hackFlexWrap)
Declaration.hack(hackGridArea)
Declaration.hack(hackPlaceSelf)
Declaration.hack(hackGridStart)
Declaration.hack(hackAlignSelf)
Declaration.hack(hackAppearance)
Declaration.hack(hackFlexBasis)
Declaration.hack(hackMaskBorder)
Declaration.hack(hackMaskComposite)
Declaration.hack(hackAlignItems)
Declaration.hack(hackUserSelect)
Declaration.hack(hackFlexShrink)
Declaration.hack(hackBreakProps)
Declaration.hack(hackWritingMode)
Declaration.hack(hackBorderImage)
Declaration.hack(hackAlignContent)
Declaration.hack(hackBorderRadius)
Declaration.hack(hackBlockLogical)
Declaration.hack(hackGridTemplate)
Declaration.hack(hackInlineLogical)
Declaration.hack(hackGridRowAlign)
Declaration.hack(hackTransformDecl)
Declaration.hack(hackFlexDirection)
Declaration.hack(hackImageRendering)
Declaration.hack(hackBackdropFilter)
Declaration.hack(hackBackgroundClip)
Declaration.hack(hackTextDecoration)
Declaration.hack(hackJustifyContent)
Declaration.hack(hackBackgroundSize)
Declaration.hack(hackGridRowColumn)
Declaration.hack(hackGridRowsColumns)
Declaration.hack(hackGridColumnAlign)
Declaration.hack(hackOverscrollBehavior)
Declaration.hack(hackGridTemplateAreas)
Declaration.hack(hackPrintColorAdjust)
Declaration.hack(hackTextEmphasisPosition)
Declaration.hack(hackTextDecorationSkipInk)
Value.hack(hackGradient)
Value.hack(hackIntrinsic)
Value.hack(hackPixelated)
Value.hack(hackImageSet)
Value.hack(hackCrossFade)
Value.hack(hackDisplayFlex)
Value.hack(hackDisplayGrid)
Value.hack(hackFilterValue)

let declsCache = new Map()

class Prefixes {
  constructor(data, browsers, options = {}) {
    this.data = data
    this.browsers = browsers
    this.options = options
    ;[this.add, this.remove] = this.preprocess(this.select(this.data))
    this.transition = new Transition(this)
    this.processor = new Processor(this)
  }

  /**
   * Return clone instance to remove all prefixes
   */
  cleaner() {
    if (this.cleanerCache) {
      return this.cleanerCache
    }

    if (this.browsers.selected.length) {
      let empty = new Browsers(this.browsers.data, [])
      this.cleanerCache = new Prefixes(this.data, empty, this.options)
    } else {
      return this
    }

    return this.cleanerCache
  }

  /**
   * Declaration loader with caching
   */
  decl(prop) {
    if (!declsCache.has(prop)) {
      declsCache.set(prop, Declaration.load(prop))
    }

    return declsCache.get(prop)
  }

  /**
   * Group declaration by unprefixed property to check them
   */
  group(decl) {
    let rule = decl.parent
    let index = rule.index(decl)
    let { length } = rule.nodes
    let unprefixed = this.unprefixed(decl.prop)

    let checker = (step, callback) => {
      index += step
      while (index >= 0 && index < length) {
        let other = rule.nodes[index]
        if (other.type === 'decl') {
          if (step === -1 && other.prop === unprefixed) {
            if (!Browsers.withPrefix(other.value)) {
              break
            }
          }

          if (this.unprefixed(other.prop) !== unprefixed) {
            break
          } else if (callback(other) === true) {
            return true
          }

          if (step === +1 && other.prop === unprefixed) {
            if (!Browsers.withPrefix(other.value)) {
              break
            }
          }
        }

        index += step
      }
      return false
    }

    return {
      down(callback) {
        return checker(+1, callback)
      },
      up(callback) {
        return checker(-1, callback)
      }
    }
  }

  /**
   * Normalize prefix for remover
   */
  normalize(prop) {
    return this.decl(prop).normalize(prop)
  }

  /**
   * Return prefixed version of property
   */
  prefixed(prop, prefix) {
    prop = vendor.unprefixed(prop)
    return this.decl(prop).prefixed(prop, prefix)
  }

  /**
   * Cache prefixes data to fast CSS processing
   */
  preprocess(selected) {
    let add = {
      '@supports': new Supports(Prefixes, this),
      'selectors': []
    }
    for (let name in selected.add) {
      let prefixes = selected.add[name]
      if (name === '@keyframes' || name === '@viewport') {
        add[name] = new AtRule(name, prefixes, this)
      } else if (name === '@resolution') {
        add[name] = new Resolution(name, prefixes, this)
      } else if (this.data[name].selector) {
        add.selectors.push(Selector.load(name, prefixes, this))
      } else {
        let props = this.data[name].props

        if (props) {
          let value = Value.load(name, prefixes, this)
          for (let prop of props) {
            if (!add[prop]) {
              add[prop] = { values: [] }
            }
            add[prop].values.push(value)
          }
        } else {
          let values = (add[name] && add[name].values) || []
          add[name] = Declaration.load(name, prefixes, this)
          add[name].values = values
        }
      }
    }

    let remove = { selectors: [] }
    for (let name in selected.remove) {
      let prefixes = selected.remove[name]
      if (this.data[name].selector) {
        let selector = Selector.load(name, prefixes)
        for (let prefix of prefixes) {
          remove.selectors.push(selector.old(prefix))
        }
      } else if (name === '@keyframes' || name === '@viewport') {
        for (let prefix of prefixes) {
          let prefixed = `@${prefix}${name.slice(1)}`
          remove[prefixed] = { remove: true }
        }
      } else if (name === '@resolution') {
        remove[name] = new Resolution(name, prefixes, this)
      } else {
        let props = this.data[name].props
        if (props) {
          let value = Value.load(name, [], this)
          for (let prefix of prefixes) {
            let old = value.old(prefix)
            if (old) {
              for (let prop of props) {
                if (!remove[prop]) {
                  remove[prop] = {}
                }
                if (!remove[prop].values) {
                  remove[prop].values = []
                }
                remove[prop].values.push(old)
              }
            }
          }
        } else {
          for (let p of prefixes) {
            let olds = this.decl(name).old(name, p)
            if (name === 'align-self') {
              let a = add[name] && add[name].prefixes
              if (a) {
                if (p === '-webkit- 2009' && a.includes('-webkit-')) {
                  continue
                } else if (p === '-webkit-' && a.includes('-webkit- 2009')) {
                  continue
                }
              }
            }
            for (let prefixed of olds) {
              if (!remove[prefixed]) {
                remove[prefixed] = {}
              }
              remove[prefixed].remove = true
            }
          }
        }
      }
    }

    return [add, remove]
  }

  /**
   * Select prefixes from data, which is necessary for selected browsers
   */
  select(list) {
    let selected = { add: {}, remove: {} }

    for (let name in list) {
      let data = list[name]
      let add = data.browsers.map(i => {
        let params = i.split(' ')
        return {
          browser: `${params[0]} ${params[1]}`,
          note: params[2]
        }
      })

      let notes = add
        .filter(i => i.note)
        .map(i => `${this.browsers.prefix(i.browser)} ${i.note}`)
      notes = utils.uniq(notes)

      add = add
        .filter(i => this.browsers.isSelected(i.browser))
        .map(i => {
          let prefix = this.browsers.prefix(i.browser)
          if (i.note) {
            return `${prefix} ${i.note}`
          } else {
            return prefix
          }
        })
      add = this.sort(utils.uniq(add))

      if (this.options.flexbox === 'no-2009') {
        add = add.filter(i => !i.includes('2009'))
      }

      let all = data.browsers.map(i => this.browsers.prefix(i))
      if (data.mistakes) {
        all = all.concat(data.mistakes)
      }
      all = all.concat(notes)
      all = utils.uniq(all)

      if (add.length) {
        selected.add[name] = add
        if (add.length < all.length) {
          selected.remove[name] = all.filter(i => !add.includes(i))
        }
      } else {
        selected.remove[name] = all
      }
    }

    return selected
  }

  /**
   * Sort vendor prefixes
   */
  sort(prefixes) {
    return prefixes.sort((a, b) => {
      let aLength = utils.removeNote(a).length
      let bLength = utils.removeNote(b).length

      if (aLength === bLength) {
        return b.length - a.length
      } else {
        return bLength - aLength
      }
    })
  }

  /**
   * Return unprefixed version of property
   */
  unprefixed(prop) {
    let value = this.normalize(vendor.unprefixed(prop))
    if (value === 'flex-direction') {
      value = 'flex-flow'
    }
    return value
  }

  /**
   * Return values, which must be prefixed in selected property
   */
  values(type, prop) {
    let data = this[type]

    let global = data['*'] && data['*'].values
    let values = data[prop] && data[prop].values

    if (global && values) {
      return utils.uniq(global.concat(values))
    } else {
      return global || values || []
    }
  }
}

module.exports = Prefixes
